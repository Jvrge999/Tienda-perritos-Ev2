services:
  db:
    image: mysql:8
    container_name: tienda-db
    environment:
      MYSQL_ROOT_PASSWORD: admin123
      MYSQL_DATABASE: tienda_perritos
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - tienda-network

  backend-despachos:
    image: jucwd999/tienda-backend-despachos:latest
    container_name: back-despachos
    ports:
      - "3001:3001"
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=admin123
      - DB_NAME=tienda_perritos
    depends_on:
      - db
    networks:
      - tienda-network

  backend-ventas:
    image: jucwd999/tienda-backend-ventas:latest
    container_name: back-ventas
    ports:
      - "3002:3002"
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=admin123
      - DB_NAME=tienda_perritos
    depends_on:
      - db
    networks:
      - tienda-network

  frontend:
    image: jucwd999/tienda-frontend:latest
    container_name: front-tienda
    ports:
      - "80:80"
    depends_on:
      - backend-despachos
      - backend-ventas
    networks:
      - tienda-network

networks:
  tienda-network:
    driver: bridge

volumes:
  db_data: